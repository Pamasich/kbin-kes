/**
 * [mod description here]
 * @param {boolean} isActive Whether the mod has been turned on
 */
function setupCancelReplyButton (isActive) { // eslint-disable-line no-unused-vars
    if (isActive) setup();
    else teardown();

    function setup () {

    }

    function teardown () {

    }
}